# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# Data & artifacts (keep repo light; document how to obtain them)
dataset/
datasets/
*.mp4
*.avi
*.mov

# Model weights & exports
weights/*.pt
weights/*.onnx
*.engine
# Base checkpoints auto-downloaded by Ultralytics into the repo root
yolov8*.pt

# Training / benchmark outputs
runs/
results/*.csv
results/*.json
results/*.png

# OS / IDE
.DS_Store
.idea/
.vscode/

# Local demo scaffolding (synthetic data for rehearsal, not the real submission)
configs/demo_dataset.yaml
scripts/make_demo_data.py
